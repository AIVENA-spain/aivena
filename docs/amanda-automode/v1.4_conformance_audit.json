{
 "confirmed": [
  {
   "lens": "contract-conformance",
   "severity": "must_fix",
   "what": "The incident runbook claims a ticket-answer relay path that does not exist in P0: 'answer the amanda_question task; Amanda relays on the next inbound'. There is NO code path to record an answer (nothing writes answer_raw/answered_at, no endpoint, no dashboard surface), the queue kind 'ticket_answered' is explicitly skipped by processTurnDb ('kind_ticket_answered_not_engine_v1'), and the next-inbound context only injects OPEN tickets as 'still waiting' \u2014 so after an agent answers the buyer manually, Amanda may keep telling the buyer she is still waiting on the office.",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md (symptom table, last row) vs /Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/process-turn-db.ts (kind skip + openTicketNote query)",
   "expected": "Runbook tells Christian what actually happens: the agent answers the buyer directly from the inbox (human reply, ingested as agent ground truth); the amanda_questions ticket has no answer/relay mechanics until P2 ping spine, and stays open.",
   "actual": "Runbook states Amanda relays the answer on the next inbound \u2014 false as coded; the ticket can never transition to 'answered' in P0.",
   "suggested_action": "One-line runbook fix (reply directly from the Inbox; relay + ticket close land with P2 ping spine) \u2014 and either stop injecting 'still waiting' once a human agent has replied after the ticket was filed, or note that behavior.",
   "confirmed": true,
   "reason": "CONFIRMED on every element. (1) INCIDENT_RUNBOOK.md line 27 claims '/tasks \u2192 answer the amanda_question task; Amanda relays on the next inbound' \u2014 but worktree-wide grep proves answer_raw/answered_at exist ONLY as schema columns (migration 20260826220000 lines 193/196), no UPDATE amanda_questions exists in any TS/SQL, and the /tasks dashboard surface (tasks-workspace/model/actions) has zero references to amanda_question or any answer affordance \u2014 the instruction is unfollowable as written. (2) process-turn-db.ts line 135 skips any non-message/media kind (reason kind_ticket_answered_not_engine_v1), and nothing anywhere enqueues kind 'ticket_answered' \u2014 it exists only in the schema CHECK. (3) Lines 84-88 + 126-128 inject only status IN (open,clarifying,escalated) tickets as 'Q{n} to the office: ... \u2014 still waiting' into the prompt (prompt.ts line 111), and since no code can ever change ticket status, the note persists on every future inbound even after a human answered. (4) DESIGN_v1.2 and GO_LIVE_PACK both honestly place the relay pipeline + amanda_answer_relay_v1 template in P2 \u2014 the runbook contradicts the code AND the design's own phasing. One softening nuance, which does not refute the finding: an agent's direct inbox reply IS surfaced to the model as role 'agent' ground truth in recentTurns (process-turn-db.ts 114-121), so Amanda may not literally repeat 'still waiting' \u2014 but that path (reply directly from the Inbox) is exactly what the runbook should say and currently does not. For a non-coder founder running incidents from this runbook, the line promises a resolution mechanism that does not exist; the one-line fix plus a note on the persisting still-waiting injection is the correct remedy."
  },
  {
   "lens": "contract-conformance",
   "severity": "must_fix",
   "what": "A cluster of \u00a74/\u00a72/\u00a71 design-law machinery is absent AND unrecorded \u2014 named in no trigger ledger (\u00a711b), go-live pack, or README entry (grep-verified): (a) per-conversation lease + 12s debounce + abort-on-new-inbound mid-loop + booking snapshot verification \u2014 the queue lease is per-ROW, so a two-message burst becomes two turns and two instances can run concurrent turns on one conversation; (b) circuit breaker on model errors -> auto-downgrade + pre-written holding message (a repeatedly-failing turn just retires to 'failed' silently until P1 alerting); (c) durable turn journal of tool effects for replay-after-crash; (d) \u00a72 property staleness guard (45-day hedge); (e) reschedule_viewing/cancel_viewing have no path at all \u2014 the pending_actions CHECK admits them but no tool or flow creates them, so a typed cancellation can only become ask_agency/handoff by model choice.",
   "where": "DESIGN \u00a74 bullets 6/9 + \u00a72 staleness + \u00a71 tools list vs /Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/ (no lease/debounce/breaker code) and docs/amanda-automode/GO_LIVE_PACK.md + README.md (no mention)",
   "expected": "Design law: every deferral is named with an objective trigger (the \u00a711b pattern the doc itself established).",
   "actual": "These five are silent gaps \u2014 the only deferral class the records do not cover. None break a P0 safety law (single drain latch per process + EXCLUDE + idempotency contain the worst), but P0.5 is billed as burn-in of exactly this machinery.",
   "suggested_action": "Add one 'P0 deferrals beyond \u00a711b' paragraph to the go-live pack or engine README naming each with its phase (ordering machinery + breaker before P2 assisted-auto; staleness guard P1; reschedule/cancel before P2); no code change required for conformance.",
   "confirmed": true,
   "reason": "CONFIRMED on all five elements, against code + records. (a) Ordering machinery: the only lease is per queue ROW (pick_and_claim_amanda_inbound, migration 20260826220000 lines 286-311, FOR UPDATE SKIP LOCKED per row, no per-conversation fence); grep 'debounce' = 0 hits in code; process-turn-db.ts never aborts on newer inbound mid-loop; executeBooking re-checks only the pending action, not the inbound snapshot. The archived v1.3 review JSON itself corroborates verbatim: 'fences only per-row \u2026 never per-conversation \u2014 a lead's second message is claimable by the next overlapping tick mid-turn \u2026 No debounce/conversation-lease exists in the built code' \u2014 and that finding's 8f01fac fix was the recordLeadIntel race + drain latch, not the lease/debounce. (b) No circuit breaker/auto-downgrade/holding message anywhere; failing rows retire to status='failed' after MAX_ATTEMPTS with only console.error (outbox-worker.ts:53-83); the recorded P1 comment covers the \u00a77 sweep alert, a different design-sanctioned item. (c) 'journal' exists only in golden-test FakeBackends (harness.ts); prod crash-replay leans on idempotency keys, and the v1.3-found tool-loop re-execution was fixed by per-tool dedupe, not a journal. (d) No 45-day property staleness hedge in gates/prompt/backends; \u00a711b's >60d item is lead-state, distinct. (e) tools.ts registers 8 tools with no reschedule_viewing/cancel_viewing, prompt.ts has zero mentions, the only pending-action INSERT hardcodes 'book_viewing' (backends-db.ts:195-197) while the CHECK admits all three; the runbook routes reschedule/cancel to manual /viewings. Unrecorded verified: \u00a711b ledger, GO_LIVE_PACK (records only one-tap execute + atomic send gate), engine README (P2 list), and all in-code deferral comments name other deferrals but none of these five. Minor nits that do not refute: the \u00a78 P0.5 billing sentence explicitly names pending_action/EXCLUDE/date-resolver/idempotency (all of which DO exist) rather than the ordering machinery, so 'exactly this machinery' slightly overstates; and cross-instance concurrency is architectural (per-row lease) but low-probability on today's single-instance Railway deploy. Suggested action stands: one 'P0 deferrals beyond \u00a711b' paragraph in GO_LIVE_PACK.md or the engine README naming each item with its phase; no code change required for conformance."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "Go-live pack Step 2 behavior matrix overstates: 'agencies amanda_mode off/shadow -> IDENTICAL behavior to v10 (W4C keeps drafting) + queue rows written' \u2014 but EF v3 enqueues ONLY when amanda_mode != 'off' (a deliberate review fix). Same nuance hits runbook Step 0: per-agency mode='off' means inbound during the off period is never queued, so re-enabling does NOT back-process the gap (agents do see everything in the Inbox, so nothing is lost to humans \u2014 but 'processing resumes where it stopped' only holds for the Railway env switch).",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/GO_LIVE_PACK.md Step 2 + INCIDENT_RUNBOOK.md Step 0 vs supabase/functions/twilio-whatsapp-inbound/index.v3-proposed.ts line 255",
   "expected": "off -> v10-identical, NO queue rows; shadow -> v10-identical + queue rows; runbook distinguishes the two kill switches' recovery semantics.",
   "actual": "Both docs blur off and shadow into one claim.",
   "suggested_action": "Two-word fix in the pack ('off/shadow' -> 'shadow'; off = no rows) and one sentence in the runbook on the per-agency-off gap.",
   "confirmed": true,
   "reason": "CONFIRMED against code and both docs. EF v3 (index.v3-proposed.ts line 255) inserts amanda_inbound_queue rows only when amandaMode !== 'off' \u2014 a deliberate review fix (inline comment cites unbounded accumulation; originating finding present in v1.3_p0_code_review.json). Exhaustive search found no other queue INSERT, no DB trigger, and no backfill path anywhere in the worktree, so nothing rescues the docs' claims. GO_LIVE_PACK.md Step 2 states verbatim \"agencies amanda_mode off/shadow \u2192 IDENTICAL behavior to v10 (W4C keeps drafting) + queue rows written\" \u2014 false for 'off' (no rows). INCIDENT_RUNBOOK.md Step 0 covers both kill switches with one blanket claim (\"...queue up... processing resumes where it stopped when re-enabled\") that holds only for the Railway AMANDA_ENGINE_ENABLED switch; under per-agency mode='off', inbound during the off period is stored (Inbox complete, nothing lost to humans) but never enqueued, so re-enabling does not back-process the gap. Suggested fix is right-sized: change \"off/shadow\" to \"shadow\" (adding \"off = no rows\") in the pack, and add one sentence to the runbook distinguishing the two switches' recovery semantics; the runbook's \"W4C drafting continues for off/shadow\" sentence is accurate and needs no change."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "\u00a78 P0 schema scope promises 'disclosure/consent columns'; the migration delivers disclosure/mute/claim on conversations but NO consent_service/consent_marketing split anywhere (live DB check: only the legacy consent_log table exists; zero consent columns on leads). Harmless until P2 (engine is reply-only; marketing consent is gated on Christian item #3), but the halving of the promise is recorded nowhere.",
   "where": "DESIGN \u00a75 consent split + \u00a78 P0 line vs /Users/christianscholte/aivena-p2am/supabase/migrations/20260826220000_amanda_automode_p0_schema.sql (section 11)",
   "expected": "Consent split columns in P0 schema, or a recorded deferral to the P2 property-alert/marketing lane.",
   "actual": "Disclosure columns only; deferral unrecorded (migration header honestly lists what it does contain, so the migration itself does not overstate).",
   "suggested_action": "Add the consent-split deferral to the same deferral paragraph (P2, alongside \u00a711.7 alert opt-in).",
   "confirmed": true,
   "reason": "Confirmed on all legs. DESIGN \u00a78 P0 line (line 101) promises schema \"disclosure/consent columns\" and \u00a75 (line 76) unambiguously defines that consent as a consent_service/consent_marketing split per lead \u2014 so the promise cannot be read as satisfied by disclosure columns or the legacy consent_log. The migration (20260826220000, section 11) delivers only ai_disclosure_sent_at / ai_muted_at / ai_muted_by / human_claimed_at / human_claimed_by on conversations; grep for consent over the migration, all worktree migrations, and apps/ returns zero consent_service/consent_marketing matches; live DB information_schema shows the only consent columns anywhere are consent_log.consent_method/consent_text. The migration header honestly lists its contents (no overstatement there), but the halving of the \u00a78 promise is recorded nowhere: GO_LIVE_PACK.md, INCIDENT_RUNBOOK.md, and README.md have zero consent mentions even though GO_LIVE_PACK records other P2 deferrals (one-tap EXECUTE endpoint, relay templates), and DESIGN \u00a711b's deferral ledger omits it. Harm assessment also verified: engine is reply-only through P1 shadow and marketing sends are P2-gated on Christian item #3, so this is a records-honesty gap (should_note), correctly remedied by adding the consent-split deferral to the P2 deferral paragraph alongside the \u00a711.7 alert opt-in."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "\u00a711.4 day-one data pack ('log now, loop later \u2014 unrecoverable if skipped') is only partially wired: funnel_events are emitted for viewing_booked, question_ticket, and handoff only \u2014 intel_captured, engaged, and lead_created are never written (EF v3 does not emit funnel events; recordLeadIntel does not log intel_captured); amanda_questions.question_category exists but nothing ever populates it. Most are back-derivable from other tables, so little is truly lost, but the pack was sold as complete-from-day-one.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/backends-db.ts + process-turn-db.ts (the three INSERT INTO amanda_funnel_events sites) vs DESIGN \u00a711.4",
   "expected": "All funnel_events event_types emitted from the moment the engine runs, question_category stamped at filing.",
   "actual": "3 of 14 event types emitted; question_category always NULL.",
   "suggested_action": "Cheap adds before P1 shadow (one insert in recordLeadIntel, one in EF v3 lead-create, category param on ask_agency) \u2014 or record the partial coverage.",
   "confirmed": true,
   "reason": "CONFIRMED by exhaustive read. Migration CHECK lists exactly 14 event types; a worktree-wide grep finds exactly 3 hardcoded emission sites: 'question_ticket' (backends-db.ts:278), 'handoff' (backends-db.ts:329), 'viewing_booked' (process-turn-db.ts:253). EF v3 (index.v3-proposed.ts:127-150) inserts new leads with zero funnel writes \u2014 'lead_created' and 'engaged' are never emitted; recordLeadIntel (backends-db.ts:292-315) writes amanda_lead_state without 'intel_captured'; amanda_questions.question_category (migration line 189) is written by no code. The back-derivability softening is accurate (leads.created_at, mergeExtraction per-field timestamps, amanda_pending_actions rows). One fairness nuance that does NOT refute it: the archived hunt JSON sequenced funnel_events at P0 as schema-only and question_category to P1/P2, and ~6 of the 11 missing types belong to features contractually deferred (viewing outcomes, alert_opt_in, dormant/reactivated) \u2014 but the DESIGN doc itself (\u00a711a.4) sells the pack under 'log now, loop later \u2014 unrecoverable if skipped' without ever assigning emission to a later phase, the three gaps named correspond to code paths that already run, tickets already file at P0.5 (so the category taxonomy is late by the hunt's own 'before tickets accumulate' rule), and no record (GO_LIVE_PACK.md mentions the table only in a collision check) discloses the partial coverage. should_note severity and the suggested fix (one insert in recordLeadIntel, one in EF lead-create, category param on ask_agency \u2014 or an honest partial-coverage note) are correct."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "\u00a72's false-block budget ('<2% of social turns blocked' = P2 promotion criterion) cannot be computed from what P0 persists: amanda_turn_usage stores only the outcome string \u2014 turn class (social vs fact_bearing) and gate-failure reasons live only in the in-memory TurnResult and are discarded. \u00a78 lists 'false-block budget met' as a P1->P2 gate, so the measurement hook is needed during P1 shadow.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/process-turn-db.ts turn_usage INSERT vs DESIGN \u00a72 + \u00a78 P1 gate",
   "expected": "Enough telemetry per turn to compute blocked-social-turn rate in shadow.",
   "actual": "outcome='escalated' is recorded without turn class or failure reasons (\u00a78 does put 'turn_events + rejection reasons logged' at P1, so this is borderline-recorded \u2014 but the go-live pack's P1 step doesn't name it).",
   "suggested_action": "Note it as a P1-shadow prerequisite in the pack, or add a gate_failures/turn_class column when P1 lands.",
   "confirmed": true,
   "reason": "Verified in code: amanda_turn_usage (migration lines 118-133) has no turn-class or gate-failure column, and the process-turn-db.ts INSERT (lines 385-395) writes only result.outcome \u2014 result.gateFailures is discarded, and turnClass never even reaches TurnResult (runGates returns it but turn.ts's judge keeps only failure strings; the blocked draft is also dropped, replyText: null). The only counter-path \u2014 escalateToHuman persisting 'gates_failed' + reasons into dashboard_tasks \u2014 fails in exactly the mode that matters: modes.ts line 52 simulates all non-read tools in shadow, so during P1 SHADOW (where DESIGN \u00a78 requires the false-block budget be measured before P2 promotion) nothing but the bare 'escalated' string is persisted. It is actually slightly worse than claimed: outcome='escalated' conflates gates_failed with empty_draft, so even the numerator (gate-blocked turns) is not separable, and the migration comment promises 'blocked_gate'/'handoff' outcome values the code never emits. GO_LIVE_PACK Step 5 confirms the omission: it names only 'real cost + escalation calibration' for turn_usage, with zero mentions of rejection reasons, turn class, turn_events, or false-block anywhere in the pack. Mitigation keeping this at should_note: DESIGN \u00a78/\u00a711 explicitly schedules 'turn_events + rejection reasons logged' as a P1 build item, so P0 not persisting it is by design \u2014 the conformance gap is the pack's P1 step not carrying that prerequisite, plus its mild overstatement that turn_usage alone supports 'escalation calibration'. Suggested action stands: name rejection-reason + turn-class telemetry as a P1-shadow prerequisite in GO_LIVE_PACK Step 5, or add gate_failures/turn_class columns when P1 lands."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "\u00a74 promises the EF 'writes a queue row in the same transaction as the message'; EF v3 uses two sequential supabase-js inserts (PostgREST cannot transact them). A failed queue insert leaves a stored message with no engine turn and no draft (W4C is also skipped for approval+ agencies), journaled only in webhook_events.error_message, with no alert until the P1 sweep. Platform-constrained deviation, but unrecorded.",
   "where": "/Users/christianscholte/aivena-p2am/supabase/functions/twilio-whatsapp-inbound/index.v3-proposed.ts lines 249-277 vs DESIGN \u00a74 outbox bullet",
   "expected": "Atomic message+queue write, or a recorded note that atomicity is approximated (unique key + webhook_events journal) with the P1 sweep as the safety net.",
   "actual": "Best-effort second insert; failure path is forensic-only until P1 alerting.",
   "suggested_action": "One sentence in CAPTURE_NOTES/go-live pack; consider having the P1 sweep also diff conversation_messages vs amanda_inbound_queue for on-dial agencies.",
   "confirmed": true,
   "reason": "CONFIRMED, and slightly understated. DESIGN \u00a74 line 65 promises the EF \"writes a queue row in the same transaction as the message\"; index.v3-proposed.ts performs two sequential supabase-js/PostgREST inserts (conversation_messages line 218, amanda_inbound_queue line 256) with no RPC or DB trigger providing atomicity (migration checked \u2014 none exists on conversation_messages). On queue-insert failure the message is stored, webhook_events.processing_status remains \"processed\" (set at line 244 before the queue attempt), and only error_message gets a \"queue:\" prefix (line 275) \u2014 forensic-only; W4C is also skipped for approval+ agencies (lines 286-287), so the message gets no draft path at all. The deviation is unrecorded: CAPTURE_NOTES describes \"after the message insert\" without flagging the departure; GO_LIVE_PACK's only atomicity note is the send gate; INCIDENT_RUNBOOK overstates the truth in exactly this failure mode (\"NOTHING is lost \u2014 the queue is durable\", \"Buyers are NOT lost: rows wait in amanda_inbound_queue\" \u2014 false when the queue row was never written). Two sharpenings: (a) the P1 sweep as specced (outbox-worker.ts lines 12-13, \"sweep alert on old unprocessed rows\") reads queue rows, so a missing queue row is invisible even to it \u2014 the suggested conversation_messages-vs-amanda_inbound_queue diff is the only proposed mechanism that would detect this; (b) \"platform-constrained\" is only half-true \u2014 a SECURITY DEFINER RPC (a pattern the schema already uses for pick_and_claim_amanda_inbound) or trigger could have made it atomic, so this was a design choice within the platform, strengthening the case for recording it. Severity should_note stands; the one-sentence CAPTURE_NOTES/go-live note plus fixing the runbook's \"NOTHING is lost\" line is the right remedy."
  },
  {
   "lens": "contract-conformance",
   "severity": "should_note",
   "what": "Several \u00a710 tone laws exist only as tested-but-unwired libraries or not at all: slotFilled/askableSlots (the 'filled slot never re-asked' deterministic blocker) and cooldownOk are exported and tested but nothing in the turn path calls them; 'statement-only after two consecutive answered questions' and the 20-90s jittered send pacing are not implemented; the \u00a72 no-commitment floor ('never reserved') is prompt-only rather than the promised deterministic screen (though booking-as-only-commitment IS structural). The nudge scheduler itself is properly ledger-recorded; these adjacent pieces are not.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/lead-state-lib.ts + validators.ts (cooldownOk) vs turn.ts/process-turn-db.ts (no call sites); DESIGN \u00a710 B2/B5/\u00a72",
   "expected": "Each \u00a710 sub-law either enforced in the send path or named as deferred.",
   "actual": "Enforced: length, one-question, banned patterns, payment floor. Prompt-only/unwired: re-ask blocker, cooldown, statement-only cadence, pacing, no-commitment wording screen.",
   "suggested_action": "Fold into the deferral paragraph (most become meaningful only when the scheduler/proactive sends exist, which is honest to say).",
   "confirmed": true,
   "reason": "CONFIRMED on every material sub-claim, verified by import tracing and full-runtime grep in /Users/christianscholte/aivena-p2am. (1) slotFilled/askableSlots (lead-state-lib.ts:71/79) are exported and tested but have zero runtime call sites \u2014 only mergeExtraction from that lib is wired (backends-db.ts:302 via record_lead_intel); re-ask prevention is prompt-only (prompt.ts:41,104), while DESIGN \u00a710 promises 'a filled slot is never re-asked (deterministic blocker)' and lead-state-lib.ts:70's own comment claims 'a filled slot is never asked again' above a never-called function \u2014 the sharpest honesty point. (2) cooldownOk (validators.ts:100) is tested but validateDraft (validators.ts:107-116) provably excludes it (no timestamp params) and nothing else calls it; mitigating nuance: P0 has no uninvited-send path (outbox only drains inbound replies), so the \u00a710 'scheduler refuses uninvited messages' law is vacuously satisfied until the \u00a711b-deferred nudge scheduler exists \u2014 this softens but does not refute, and matches the finding's own suggested framing. (3) Statement-only-after-two-answered-questions: no consecutive-question tracking anywhere (the one-question half IS enforced, validators.ts:54). (4) 20-90s jittered pacing (\u00a710 line 124): absent \u2014 only setTimeout is the LLM timeout (llm.ts:23); sendReply enqueues straight to send_queue (process-turn-db.ts:285-300). (5) \u00a72 no-commitment floor (design line 23 '(deterministic): never reserved'): prompt-only (prompt.ts:60) plus the narrow 'reserve today' banned pattern; booking-as-only-commitment is structural via the tool layer, exactly as the finding concedes. Records side: engine README declares 'P0 CODE-COMPLETE ... validators/.../lead-state ... law' and neither README nor GO_LIVE_PACK names any of these sub-laws as unwired; \u00a711b defers the nudge scheduler (implicitly covering cooldown's enforcement point) but not the re-ask wiring, cadence, pacing, or commitment screen. The finding's 'enforced' column is also accurate (validateDraft at turn.ts:177 covers length/mirror/one-question/banned/payment). Severity should_note is right; suggested fix (fold into a deferral paragraph \u2014 most pieces only become meaningful when proactive sends exist) is honest and cheap, and should also fix the lead-state-lib.ts:70 comment to say the blocker is not yet wired."
  },
  {
   "lens": "contract-conformance",
   "severity": "cosmetic",
   "what": "Stale/imprecise record lines: engine README's first 'State (P0, in progress)' block and the outbox-worker.ts header both still say the worker is 'NOT wired into index.ts yet' \u2014 it IS wired (env-gated, doubly inert) since commit 2d748fa; README says golden suite has '32 scenarios' (actual: 33) and '411 tests green' (branch now at 656 \u2014 counts only grew); \u00a711d says templates 'submit at build start' while the go-live pack re-times to 'build start of their phase' (visible re-scope, not silent, and the audit showing 4 of 7 already approved makes it reasonable).",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/README.md + outbox-worker.ts header comment vs apps/api/src/index.ts lines 213-231",
   "expected": "Superseded statements corrected when the later state section was added.",
   "actual": "Historical bullets left in place; a reader skimming only the top of the README gets an outdated (safer-than-real, but wrong) picture.",
   "suggested_action": "Delete or past-tense the two 'not wired yet' lines; fix the scenario count.",
   "confirmed": true,
   "reason": "CONFIRMED with one sub-claim refuted. Verified against files + git: (1) README.md lines 12-14 ('NOT wired into index.ts yet') and the outbox-worker.ts header both remain false at HEAD \u2014 index.ts lines 213-231 wire the worker (env-gated engineEnabled() block, 20s drain), landed in 2d748fa which git show --stat confirms added 22 lines to index.ts; the later 'P0 CODE-COMPLETE' README section never corrects the earlier bullet. (2) '32 scenarios' is off by one: golden/core-scenarios.test.ts has 33 it() blocks (25 at ea78588 + 8 regression scenarios in 8f01fac = 33; names S1-S25 incl. S8b + R1-R7). (3) \u00a711d 'submit at build start' vs go-live pack 'build start of their phase' re-timing exists exactly as described (DESIGN line 160 vs GO_LIVE_PACK line 66), visibly documented in commit b2b48c6 (4 of 7 templates already approved). REFUTED sub-claim: '411 tests green' is NOT stale \u2014 npx vitest run apps/api at current HEAD passes exactly 411 tests; the finding's 656 is the whole-repo count (dashboard + EF + packages, plus one APP_PW-gated RLS suite), a scope conflation, so the README's 411 is currently accurate (though unscoped in the text). Net: the two stale 'not wired yet' lines and the 32-vs-33 count truly hold; severity cosmetic is right and the error direction is safer-than-real (claims less activation than exists). Suggested fix stands minus the test-count edit: past-tense the two wiring lines, change 32\u219233, optionally annotate 411 as the apps/api-scoped count."
  },
  {
   "lens": "wish-traceability",
   "severity": "must_fix",
   "what": "INCIDENT_RUNBOOK tells Christian a recovery path that does not exist: 'Ticket (Q to office) never answered ... /tasks -> answer the amanda_question task; Amanda relays on the next inbound (full ping spine = P2)'. There is no way to answer an amanda_question task (the /tasks page only supports Resolve/dismiss - tasks-model.ts; zero API/dashboard code touches amanda_questions - grep confirms), nothing ever sets status='answered' or answer_raw, and even if it did, process-turn-db.ts loadWorld injects only status IN ('open','clarifying','escalated') tickets as 'still waiting' - an answer can never reach the buyer. In a real incident the founder would follow this line and the buyer would keep waiting.",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md line 27 (vs /Users/christianscholte/aivena-p2am/apps/dashboard/app/(app)/tasks/tasks-model.ts and /Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/process-turn-db.ts lines 84-88)",
   "expected": "The runbook (written explicitly 'for Christian, non-coder') describes only actions that actually work today.",
   "actual": "It claims an in-product answer flow and an automatic relay-on-next-inbound; neither exists in P0 - relay machinery is entirely P2.",
   "suggested_action": "Rewrite the row to the honest P0 path, e.g.: 'Open the conversation in the Inbox and answer the buyer yourself (this claims the chat from Amanda); resolve the task. Amanda cannot relay office answers until the P2 ping spine.'",
   "confirmed": true,
   "reason": "CONFIRMED on all three legs by direct file inspection. (1) INCIDENT_RUNBOOK.md line 27 verbatim tells Christian: \"/tasks \u2192 answer the amanda_question task; Amanda relays on the next inbound (full ping spine = P2)\". (2) No answer flow exists: worktree-wide grep for amanda_question hits only the migration, backends-db.ts (SELECT+INSERT only), and process-turn-db.ts (SELECT only); there is zero UPDATE on amanda_questions anywhere, nothing ever sets status='answered'/answer_raw/answered_by/answered_at (schema-only columns), the 'ticket_answered' message kind appears only in the migration CHECK, and the /tasks page (tasks-model.ts + actions.ts) implements exactly one write \u2014 dismiss with a whitelisted reason \u2014 with no answer affordance (grep for \"amanda\" in the tasks dir returns nothing; backends-db.ts's own comment says the answer surface is \"the P2 build\"). (3) Relay is impossible even hypothetically: process-turn-db.ts:84-88 loads only status IN ('open','clarifying','escalated') tickets and lines 126-127 render them solely as \"\u2014 still waiting\"; no code reads answer_raw. DESIGN_v1.2 confirms the entire answer/relay pipeline (intent classifier, \u226460s relay, \"Questions from Amanda\" reply-box queue, the 1-min ping worker) is \u00a73b ping-spine P2 \u2014 so the runbook's parenthetical understates the gap: relay-on-next-inbound itself does not exist, and with no worker and nothing setting 'expired', the ticket stays \"still waiting\" in Amanda's context indefinitely. In a real incident the non-coder founder would follow a dead path while the buyer keeps waiting. Fix as suggested (rewrite row 27 to: answer the buyer yourself in the Inbox, then resolve the task; Amanda cannot relay office answers until P2) \u2014 but verify or omit the \"this claims the chat from Amanda\" clause, as claim semantics of a manual inbox reply were not verified in this pass."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "Wish 3 (ask-agency loop): in the P0.5 FULL demo Amanda will PROMISE 'I'll check with the office and come back to you' (prompt.ts escalation ladder rung 2 instructs exactly this), but no relay/answer mechanism exists until P2 - the promise is structurally unkeepable. The GO_LIVE_PACK demo script stops at 'ticket appears on /tasks' without one line saying the comeback cannot happen yet, so Christian's dream-pattern demo is one sentence away from being oversold.",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/GO_LIVE_PACK.md Step 4 (vs prompt.ts line 52 and the absence of any relay path)",
   "expected": "The demo script names the known gap the way it already names the one-tap booking gap ('one-tap execute lands with P2').",
   "actual": "The filing half is demoed; the relay half is silently absent from the script (it IS implied elsewhere via 'amanda_answer_relay_v1 needed by P2', but not stated where the demo is described).",
   "suggested_action": "Add one line to Step 4: 'NOTE: Amanda will say she'll come back with the office's answer - that relay is the P2 ping spine; in this demo the agent answers the buyer manually from the Inbox.'",
   "confirmed": true,
   "reason": "CONFIRMED on all three legs. (1) Promise: prompt.ts:52 (rung 2) instructs verbatim \"tell them you'll check with the office and come back\"; line 44 rehearses relay openers; process-turn-db.ts:127 re-injects \"Q{n} to the office: '\u2026' \u2014 still waiting\" on every later turn \u2014 in the P0.5 FULL demo Amanda WILL make this promise. (2) No relay exists: amanda_questions is touched only by the migration, the engine write path (backends-db.ts askAgency), and the still-waiting read; no route answers a ticket; next_ping_at is written but nothing reads it (no ping worker); backends-db.ts:264-267's own comment says the Questions surface \"is the P2 build\" and task_type 'amanda_question' was chosen so the approve RPC \"can never send it to the buyer\" \u2014 the ticket is deliberately relay-proof in P0. Design doc places the ping spine/relay at P2 (\u00a73b-spine \"PROPOSAL\u2026 before the P2 build\"; P0 scope = tickets schema only). (3) Asymmetry in the record: GO_LIVE_PACK Step 4's demo script stops at \"ticket appears on /tasks (+ amanda_questions row)\" with no line that the comeback cannot happen, while the SAME document explicitly names the parallel one-tap-booking gap (\"EXECUTE endpoint does not exist yet (P2)\") and mentions the relay only obliquely in the templates section (\"amanda_answer_relay_v1 \u2026 needed by P2 ask-agency relay\"). Refutation attempts failed: the Inbox manual answer is a human keeping Amanda's promise (and is exactly the unstated workaround); \"send_queue freeform relays\" in Step 4 means engine replies to inbound turns, not office-answer relays; agency_amanda_knowledge only enables a reactive answer if the buyer asks again, not the promised proactive comeback. Severity should_note is right \u2014 it is a demo-script honesty gap, not a code defect; the suggested one-line addition to Step 4 (naming the gap the way the booking gap is named, and stating the agent answers manually from the Inbox in the demo) is the correct fix. File: /Users/christianscholte/aivena-p2am/docs/amanda-automode/GO_LIVE_PACK.md (Step 4, lines 39-51) vs /Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/prompt.ts:52 and backends-db.ts:224-290."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "Wish 2 ('go to a human if you actually don't have an answer'): the genuinely-unanswerable branch dead-ends. cannot_answer exists as a first-class tool and its schema tells the model 'the engine decides what happens next' (tools.ts line 153), but turn.ts never reads loop.cannotAnswer - no task, no ticket, no escalation results; the model's own 'I don't know' prose is simply sent. Sensitive->handoff and agency-answerable->ticket ARE built; this third branch is neither built nor explicitly phase-tagged (design section 3c operationalizes it; section 8 P1 only loosely covers it as 'escalation calibration measured here'; golden S24 honestly pins 'surfaced to the orchestrator for calibration').",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/turn.ts (no use of loop.cannotAnswer) vs /Users/christianscholte/aivena-p2am/docs/amanda-automode/DESIGN_v1.2_2026-08-26.md line 57",
   "expected": "Either cannot_answer routes somewhere (task/ticket) in P0, or a doc line states plainly that abstention routing is calibrated-then-wired at P1.",
   "actual": "Signal recorded in telemetry only; no human is ever notified when Amanda genuinely cannot answer and the topic is not agency-answerable.",
   "suggested_action": "One line in README/GO_LIVE_PACK: 'cannot_answer is telemetry-only in P0; routing (ticket vs handoff) is wired after P1-shadow calibration.' Or cheaply file a low-priority human_review_needed task on cannot_answer now.",
   "confirmed": true,
   "reason": "Verified against the actual worktree files. (1) turn.ts (full read) never consumes loop.cannotAnswer \u2014 only loop.text, loop.toolEvents, and loop.handedOff; repo grep shows no other consumer. (2) tools.ts line 153 tells the model \"the engine decides what happens next\" while the handler (line 255) is a pure no-op signal, and golden S24 proves the end state: cannot_answer \u2192 model prose validated \u2192 outcome 'sent', no task/ticket/escalation. (3) The other two branches are built (handoff_to_human and ask_agency both produce real dashboard_tasks writes in process-turn-db.ts), making this the one unrouted branch of design \u00a73c line 57. (4) Zero mentions of cannot_answer/abstention in GO_LIVE_PACK.md, docs README, or engine README \u2014 no record states the P0 telemetry-only status; DESIGN line 57's \"calibrated in shadow\" and \u00a78 P1's \"escalation calibration measured here\" are the only loose phase gestures, exactly as filed. One correction that strengthens the finding: \"recorded in telemetry only\" is generous \u2014 amanda_turn_usage persists only a tool-call count (process-turn-db.ts line 392); the cannot_answer reason string is never durably stored, so the promised P1-shadow calibration currently has no persisted signal to calibrate from. Considered refutation: prompt.ts line 47 reserves cannot_answer for facts nobody can know (agency-answerable \u2192 ask_agency), which mitigates severity to should_note but does not refute \u2014 the schema still over-promises, mis-sorted agency-answerable questions dead-end silently with no SLA, and founder-facing records are silent. Suggested one-line doc fix stands; add that the reason should also be persisted before shadow calibration."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "Wish 9 (must-not-fail): design section 4's burst/ordering machinery - 12s debounce so bursts become one turn, per-conversation lease, new-inbound-mid-loop abort+fold-in, and 'booking transactions verify the inbound snapshot hasn't advanced' - is neither built nor listed in the section 11b deferral ledger, README, or GO_LIVE_PACK. Concrete conformance gap to the quoted confirmation law ('no newer inbound modifies it', turn.ts header): buyer sends 'yes' then 3 seconds later 'wait, Sunday instead' - two queue rows; the 'yes' row books Saturday before the modifier is processed. Double-texting is normal WhatsApp behavior and the P0.5 FULL demo will exercise it.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/process-turn-db.ts + outbox-worker.ts (row-level lease only; no newer-inbound check before executeBooking) vs DESIGN_v1.2 section 4 'Locks & ordering'",
   "expected": "Built, or explicitly deferred with a trigger in section 11b like every other deferral.",
   "actual": "Silently missing from every record; 'P0 CODE-COMPLETE' reads as if section 4 landed whole.",
   "suggested_action": "Add to the deferral ledger + GO_LIVE_PACK known-gaps: 'burst debounce / newer-inbound supersession check = pre-P1'; cheapest partial fix: before executeBooking, check amanda_inbound_queue for a newer pending row on the same conversation and fall through to re-ask.",
   "confirmed": true,
   "reason": "CONFIRMED on every element, and the gap is slightly worse than stated: the code's own contract promises the missing check. (1) The promise exists: DESIGN_v1.2 line 66 (\u00a74 'Locks & ordering', the 'must not fail' section) promises per-conversation lease (TTL+heartbeat), 12s adaptive debounce so bursts become one turn, new-inbound-mid-loop abort+fold-in+restart, and booking transactions verifying the inbound snapshot hasn't advanced. (2) None of it is built: pick_and_claim_amanda_inbound (migration 20260826220000, lines 286-311) claims rows purely by created_at ASC with a per-ROW lease_expires_at \u2014 no conversation grouping, no debounce delay, rows eligible the instant they're written; outbox-worker.ts drains them sequentially; processTurnDb loads the world once with no re-check; executeBooking (process-turn-db.ts:208-257) re-verifies only the pending-action row (status='pending', unexpired) and the EXCLUDE-constraint arbiter \u2014 it never queries amanda_inbound_queue for a newer pending row on the conversation. grep for debounce/heartbeat/abort/fold across the engine + EF: zero hits. (3) The scenario holds concretely: 'yes' and 'wait, Sunday instead' 3s apart = two queue rows (one per MessageSid via turn-id); the 'yes' row is processed first and the deterministic pre-step in turn.ts (lines 97-107) books BEFORE any model call \u2014 even when the modifier row is already sitting pending in the same table. MODIFIER_RE + golden S5 handle 'yes but sunday' only within ONE message; the cross-message burst is unprotected. In FULL mode (the P0.5 demo path) that is a real booking + calendar event against a retracted confirmation. (4) The overstatement is concrete: confirmation.ts's own docstring (lines 56-58) says 'affirm' may execute 'only when the deterministic caller has verified there is exactly ONE unexpired pending action and no newer modifying inbound' \u2014 no caller anywhere performs the newer-inbound half. (5) Silently missing from every record: \u00a711b ledger lists ~10 deferrals (ping spine, episodic memory, cross-channel linking, etc.) \u2014 not this; the engine README declares 'P0 CODE-COMPLETE' and honestly defers OTHER \u00a74 items (atomic send gate \u2192 P2, one-tap endpoint \u2192 P2) \u2014 not this; GO_LIVE_PACK has explicit 'does not exist yet' notes for those same other items \u2014 not this. Calibration (why should_note, not blocker): pre-P2 blast radius is the is_test agency demo only (P1 is SHADOW, bookings simulated), and within one worker process rows are at least processed in created_at order. Suggested action is right and cheap: add the gap to \u00a711b + GO_LIVE_PACK known-gaps, and the one-query partial fix (check amanda_inbound_queue for a newer pending/processing row on the conversation before executeBooking, fall through to re-ask) is directly implementable at process-turn-db.ts:208."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "reschedule_viewing / cancel_viewing are in the design's tool list (section 1) and even in the pending_actions action_type CHECK, but absent from TOOL_SPECS with no deferral note anywhere. A buyer's 'can we move Saturday's viewing?' has no tool path - the model will improvise (ask_agency or handoff at best). The P0.5 demo script books a viewing; the natural next demo message is a reschedule.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/tools.ts TOOL_SPECS vs DESIGN_v1.2 line 14 and migration line 57",
   "expected": "Tools present, or one line in README/GO_LIVE_PACK deferring them to a named phase.",
   "actual": "Silently missing.",
   "suggested_action": "Add 'reschedule/cancel viewing tools = P2 (agent handles via /viewings until then)' to the README next-steps list.",
   "confirmed": true,
   "reason": "Verified against the actual files. DESIGN_v1.2 line 14 promises reschedule_viewing and cancel_viewing as tools (the design's only mention of cancel_viewing \u2014 no phase tag), and migration line 57's pending_actions CHECK includes both action types. But tools.ts TOOL_SPECS defines exactly 8 tools with neither present; unlike book_viewing, whose absence carries an explicit design comment (tools.ts:5-7, executes deterministically from a confirmed pending action), reschedule/cancel have NO execution path at all \u2014 backends-db.ts:197 creates only action_type='book_viewing' pending actions, and nothing creates or executes the other two types. modes.ts:32 even names them in a comment as commitment-class tools. No deferral note exists anywhere: engine README's next-steps/P2 list, docs/amanda-automode/README.md, GO_LIVE_PACK.md, and the design's \u00a711b build-when ledger all omit them; prompt.ts gives the model no guidance for a move/cancel request; the golden suite has zero reschedule scenarios. Best refutation candidates fail: prompt.ts:52 routes 'viewing exceptions' to ask_agency (exactly the improvised fallback the finding predicted, not a documented decision), and INCIDENT_RUNBOOK.md:24's manual /viewings fix is incident response, not a stated deferral. Severity 'should_note' is correctly calibrated \u2014 the gap fails soft via ask_agency, but the record silently overstates capability, and the P0.5 demo (which books a viewing) makes a buyer reschedule request a near-certain early encounter. The suggested README one-liner is the right fix."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "INCIDENT_RUNBOOK's 'can NEVER happen by construction' list attributes the 24h-window refusal to the executor ('outside the 24h window (executor refuses freeform)'), but the code's own reviewer-verified comment says the LIVE executor enforces opt-out ONLY - the window check is the engine's enqueue-time pre-check (23h30m) plus a 30-min queue expiry, which in the worst case lands exactly at the 24h boundary rather than the design section 4's promised 60-120s safety margin.",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md line 31 vs /Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/process-turn-db.ts lines 286-298 and GO_LIVE_PACK 'the executor enforces opt-out' law",
   "expected": "The never-happen list matches the actual enforcement point (this doc is exactly where honesty matters most).",
   "actual": "Attributes the guarantee to a gate the pack itself says does not exist until P2; margin can be ~0 at the boundary.",
   "suggested_action": "Reword to 'engine refuses to enqueue freeform in the last 30 min of the window; the atomic executor-side gate lands before P2', and/or tighten the pre-check to 23h to restore a real margin.",
   "confirmed": true,
   "reason": "CONFIRMED. INCIDENT_RUNBOOK.md line 31 verbatim reads \"A send to an opted-out lead (executor refuses) or outside the 24h window (executor refuses freeform)\" in the \"can NEVER happen by construction\" list \u2014 attributing the window refusal to the executor. The code (process-turn-db.ts:285-298) and GO_LIVE_PACK (lines 48, 84-86) both state the LIVE executor enforces opt-out ONLY (reviewer-verified), with the 24h check being an engine-side enqueue pre-check (23h30m) plus a 30-min send_queue expiry; the atomic executor-side window gate is explicitly deferred to before P2. Adversarial check of the executor source itself (supabase/functions/whatsapp-send-execute/index.ts) confirms: the only pre-send refusal is the opt-out guard (line 228); the only \"24h window\" text (lines 419-420) is a hint attached AFTER Twilio rejects with error 63016 \u2014 Twilio refuses, not the executor. Margin arithmetic holds: enqueue can pass at up to 23h29m59s after last inbound and the queue may drain up to 30 min later, landing ~exactly at the 24h boundary \u2014 versus design section 4's promised 60-120s safety margin (DESIGN_v1.2 line 67), which belongs to the deferred P2 gate. Partial mitigation, not refutation: the buyer-visible OUTCOME is still prevented because Meta/Twilio block out-of-window freeform at the platform (boundary overshoot = failed send, a lost reply, not a compliance breach) \u2014 but the runbook's attribution is wrong for the build state it documents, in the document a non-coder founder relies on during incidents (and line 25 correctly attributes opt-out to the executor, making line 31 look deliberate). Severity should_note is right; the suggested rewording and/or tightening the pre-check to 23h is accurate."
  },
  {
   "lens": "wish-traceability",
   "severity": "should_note",
   "what": "Wish 8 ('our standard and then they can adjust and add'): only the read half exists. No write surface for agency knowledge (no route touches agency_amanda_knowledge; the save-time scrubber is unimplemented) and the section 6 settings card has no phase tag in the design, README, or pack. Additionally parseAmandaSettings hardcodes viewingHoursByWeekday (11:00/17:00 + Sat 11:00) and process-turn-db hardcodes the prompt's workingHoursLine, while the migration comment says amanda_settings holds 'working hours/duration/escalation phone/tone/quiet-hours etc.' - implying adjustability that is not yet real (timezone/duration/notice ARE read from settings).",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/backends-db.ts lines 21-30, process-turn-db.ts line 186, migration line 268",
   "expected": "Either a phase tag for the settings/knowledge write surface, or the migration comment scoped to what the engine actually reads today.",
   "actual": "Schema-ready read side presented next to comments implying agencies can already adjust hours/tone; they cannot, and no doc says when they will.",
   "suggested_action": "Add 'section 6 settings card + knowledge save-time screen = P2' to the README phase list; note in the migration or README which settings keys the engine reads in P0 (timezone, viewing_duration_min, viewing_notice_hours).",
   "confirmed": true,
   "reason": "Verified in the worktree: (1) only the read half of Wish 8 exists \u2014 process-turn-db.ts:90-94 SELECTs agency_amanda_knowledge into the prompt, but repo-wide grep finds no route touching the table, no code writing amanda_settings, and zero implementation of the promised save-time scrubber (design lines 78/84/157) \u2014 though with no write surface the scrubber gap is currently fail-safe. (2) No phase tag anywhere: README's P2 list (lines 35-36) omits the settings card and knowledge screen; design \u00a78 phases mention only the knowledge table (P0) and review feed (P2), never the \u00a76 settings card; GO_LIVE_PACK references the table only in a schema-verification query. (3) backends-db.ts:28 hardcodes viewingHoursByWeekday ignoring input while lines 25-27 do read timezone/viewing_duration_min/viewing_notice_hours; process-turn-db.ts:186 hardcodes the workingHoursLine text. (4) Migration line 268 comment 'working hours/duration/escalation phone/tone/quiet-hours etc.' overstates: of those five keys the engine reads only duration \u2014 working hours are hardcoded and escalation phone/tone/quiet-hours are read nowhere. Suggested fix stands: add the settings-card/knowledge-screen phase tag to the README phase list and scope the migration comment (or README) to the three keys actually read in P0 (timezone, viewing_duration_min, viewing_notice_hours)."
  },
  {
   "lens": "wish-traceability",
   "severity": "cosmetic",
   "what": "Env-var names diverge from the design contract: design section 1 promises MODEL_ID_CONVERSATION / MODEL_ID_VERIFIER; the code uses AMANDA_ENGINE_MODEL / AMANDA_ENGINE_VERIFIER_MODEL. During a model rollback someone following the design doc would set variables that do nothing.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/llm.ts lines 14-15 vs DESIGN_v1.2 line 12",
   "expected": "Names match, or the design line is updated to the real names.",
   "actual": "Different names, both undocumented in the runbook.",
   "suggested_action": "Update the design line to the real env names (and consider adding them to the incident runbook's Railway section).",
   "confirmed": true,
   "reason": "Verified in the worktree: DESIGN_v1.2_2026-08-26.md line 12 promises env vars MODEL_ID_CONVERSATION / MODEL_ID_VERIFIER, but apps/api/src/amanda-engine/llm.ts lines 14-15 read AMANDA_ENGINE_MODEL / AMANDA_ENGINE_VERIFIER_MODEL, and a repo-wide grep confirms no code reads the design-doc names (no alias/fallback) \u2014 setting them during a rollback would silently do nothing while the hardcoded defaults keep running. Also confirmed the second half of the claim: INCIDENT_RUNBOOK.md and GO_LIVE_PACK.md document only AMANDA_ENGINE_ENABLED; neither name-set for the model vars appears in the runbook, go-live pack, or the engine README. Severity \"cosmetic\" is fair since defaults fail safe and the runbook routes model-quality incidents to CC rather than to founder env edits, but the fix (update design line 12 to the real names; add them to the runbook Railway section) is warranted."
  },
  {
   "lens": "wish-traceability",
   "severity": "cosmetic",
   "what": "README says 'golden suite (32 scenarios)'; core-scenarios.test.ts contains 33 it() scenarios. Harmless undercount, but the founder's trust rides on numbers matching exactly.",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/README.md line 25",
   "expected": "33 (verified by grep and by running the suite).",
   "actual": "32.",
   "suggested_action": "Change to 33 (or 'core-scenario suite').",
   "confirmed": true,
   "reason": "README.md line 25 says \"golden suite (32 scenarios ...)\" but golden/core-scenarios.test.ts holds exactly 33 it() scenarios (S1-S15 incl. inserted S8b, R1-R7, S16-S25; zero skip/todo/only), and running the README's own command yields \"Tests 33 passed (33)\". Refutation attempts failed: harness.ts defines no alternate 32-item scenario list, and no other file repeats the number. Likely cause: S8b added after the count was written. Undercount (suite bigger than claimed), so cosmetic \u2014 fix line 25 to 33."
  },
  {
   "lens": "records-accuracy",
   "severity": "must_fix",
   "what": "Runbook instructs an incident action that does not exist in P0: answering an amanda_question ticket and having Amanda relay it",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md line 27 (symptom row: 'Ticket (Q to office) never answered')",
   "expected": "'/tasks \u2192 answer the amanda_question task; Amanda relays on the next inbound (full ping spine = P2)' \u2014 implies an answer path exists today and the buyer will get the answer",
   "actual": "No answer-capture mechanism exists anywhere in the P0 build: /tasks offers only Resolve (dismiss RPC); nothing in the codebase writes amanda_questions.answer_raw or status='answered' (grep across apps/api, apps/dashboard, EF finds zero writers outside the migration DDL); the world loader (process-turn-db.ts:85-88) injects only open/clarifying/escalated tickets, so Amanda can hold but can never see or relay an answer. The relay template amanda_answer_relay_v1 is itself listed in GO_LIVE_PACK.md as 'needed by P2'. A non-coder following this row during a live incident would search for an answer action that is not there and believe the waiting buyer will be answered automatically \u2014 they will not; the agent must reply manually in the Inbox.",
   "suggested_action": "Rewrite the row to today's truth: 'answer the buyer yourself from the Inbox (take-over), then Resolve the task; automatic relay of office answers arrives with P2'. Keep the ping-spine parenthetical.",
   "confirmed": true,
   "reason": "Confirmed on every point by direct inspection of the worktree. INCIDENT_RUNBOOK.md:27 verbatim instructs \"/tasks \u2192 answer the amanda_question task; Amanda relays on the next inbound\" \u2014 but (1) /tasks has exactly one server action, dismissTaskAction (apps/dashboard/app/(app)/tasks/actions.ts \u2192 dismiss_dashboard_task RPC); no answer affordance exists anywhere in the dashboard; (2) amanda_questions is touched by only 3 files in the entire worktree \u2014 the migration (DDL only), backends-db.ts (SELECT+INSERT only), process-turn-db.ts (SELECT only) \u2014 and a worktree-wide grep finds zero UPDATE statements, so nothing can write answer_raw or status='answered'; (3) the world loader (process-turn-db.ts:85-88) injects only open/clarifying/escalated tickets with short_code+question_text, so Amanda could never see or relay an answer even if one were captured manually; (4) the build's own code comment says the \"Questions from Amanda\" surface is the P2 build, and GO_LIVE_PACK.md:68 lists the relay template amanda_answer_relay_v1 as \"needed by P2\". The runbook row overstates P0: during an incident the agent must reply manually in the Inbox; the suggested rewrite matches today's truth."
  },
  {
   "lens": "records-accuracy",
   "severity": "should_note",
   "what": "Runbook misattributes the 24h-window guarantee to the executor",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md line 31 ('What can NEVER happen by construction')",
   "expected": "'A send ... outside the 24h window (executor refuses freeform)'",
   "actual": "The executor enforces only the opt-out/blocked guard \u2014 the repo's OWN confirmed review finding (v1.3_p0_code_review.json, integration-truth minor) states this, and the executor's only 24h text is an error hint mapped from Twilio code 63016 AFTER Twilio itself rejects the send (whatsapp-send-execute/index.ts:418-422). The end-to-end guarantee actually holds via the engine's 23h30m enqueue pre-check (process-turn-db.ts:294-299) + 30-min expiry_at + Twilio's platform rejection \u2014 but the named mechanism is wrong, and GO_LIVE_PACK.md states the split correctly ('opt-out law there; the engine pre-checks the 24h window at enqueue').",
   "suggested_action": "Change the parenthetical to '(engine pre-checks at enqueue; Twilio rejects out-of-window freeform)' so the runbook matches the review-verified reality.",
   "confirmed": true,
   "reason": "Verified against all cited files. INCIDENT_RUNBOOK.md line 31 does claim \"outside the 24h window (executor refuses freeform)\" under \"What can NEVER happen by construction,\" but whatsapp-send-execute/index.ts contains no pre-send 24h check \u2014 its only pre-send guard is sendBlockedByOptIn (lines 227-229), and its only 24h text is the hint at lines 419-421, generated AFTER Twilio itself rejects with code 63016. The repo's own confirmed review finding (v1.3_p0_code_review.json confirmed[34], verified against repo v6.7 AND live v6.6/v20) states the executor enforces opt-out only. The actual window mechanism lives in the engine: process-turn-db.ts lines 293-298 (23h30m last_inbound_whatsapp_at pre-check at enqueue, throws window_closed) plus 30-min expiry_at (line 304), with Twilio's platform rejection as backstop \u2014 and GO_LIVE_PACK.md lines 48/86 state this split correctly, making the runbook internally inconsistent with the go-live pack. Refutation angle considered (reading \"executor refuses\" loosely as \"the send fails at the executor stage\") fails because the section defines by-construction guarantees and tells the non-coder founder which component's breach constitutes a code bug \u2014 misattributing the mechanism misdirects incident response. The end-to-end guarantee does hold, so severity should_note (not blocking) is right, and the suggested parenthetical fix \"(engine pre-checks at enqueue; Twilio rejects out-of-window freeform)\" matches the review-verified reality."
  },
  {
   "lens": "records-accuracy",
   "severity": "should_note",
   "what": "Global kill switch is presented as 'always safe' but silently zero-drafts approval+ agencies",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/INCIDENT_RUNBOOK.md Step 0 (lines 4-16)",
   "expected": "'Step 0 stops everything safely ... agents keep working exactly as before her ... W4C drafting continues for off/shadow agencies'",
   "actual": "EF v3 suppresses W4C purely on amanda_mode (index.v3-proposed.ts:279-287 \u2014 engineIsDrafter is mode-based, independent of the Railway env flag). So AMANDA_ENGINE_ENABLED=false with an agency still at approval/assisted/full = nobody drafts for that agency (the exact 'silent zero-draft state' GO_LIVE_PACK's ROLLOUT ORDER law names, in reverse). The runbook never tells Christian to also dial affected agencies back to 'off' when using the global switch. Data is not lost and per-agency Step 0 is genuinely safe, but 'agents keep working exactly as before' overstates the global case.",
   "suggested_action": "Add one line to the global-off instructions: 'If any agency was at approval or higher, also run the per-agency Step 0 for it \u2014 otherwise its drafts stop entirely until re-enabled.'",
   "confirmed": true,
   "reason": "Verified in code and docs: EF v3 (index.v3-proposed.ts:286-287) suppresses W4C purely on amanda_mode with no visibility of AMANDA_ENGINE_ENABLED (only AMANDA_W4C_DISABLED is checked), and the engine worker exists only when the env flag is 'true' (apps/api/src/index.ts:220, outbox-lib.ts:26) \u2014 so global-off with any agency at approval/assisted/full = zero drafters for that agency, the exact 'silent zero-draft state' GO_LIVE_PACK.md:78-80 names as law, in reverse. INCIDENT_RUNBOOK.md lines 4-5 frame Step 0 as 'always safe ... agents keep working exactly as before her', and no line instructs dialing affected agencies back to 'off' after the global switch; the symptom table even prescribes 'Step 0 GLOBAL' (rows 2 and 5) while agencies stay at assisted/full, and the opt-out-breach row can fire with a healthy engine. Refinement, not refutation: the global paragraph itself is literally precise ('W4C drafting continues for off/shadow agencies') and data durability claims are honest \u2014 the overstatement is confined to the headline safety framing plus the missing dial-down instruction, so the one-line suggested_action is the right fix and should_note is the right severity."
  },
  {
   "lens": "records-accuracy",
   "severity": "should_note",
   "what": "Golden scenario count stale in two records (undercount, not overstatement)",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/README.md line 25 and the Drive changelog 2026-08-26/27 entry item (6)",
   "expected": "'golden suite (32 scenarios ...)' / '32 scenarios, one command, 411 tests green'",
   "actual": "core-scenarios.test.ts contains 33 scenarios (S1\u2013S25 + S8b + R1\u2013R7; verified by listing every it() block and by the vitest run: '33 tests' for that file). The 411 figure IS exact (npx vitest run apps/api \u2192 411 passed) and the one command works.",
   "suggested_action": "Bump '32' to '33' in the README and (per append-only convention) correct it in the next changelog entry rather than editing the old one.",
   "confirmed": true,
   "reason": "Verified live: golden/core-scenarios.test.ts contains 33 it() blocks (S1\u2013S25 + S8b + R1\u2013R7; no skip/todo/each), and running the documented command `npx vitest run apps/api/src/amanda-engine/golden/` reports 33 passed (33); full `npx vitest run apps/api` reports exactly 411 passed (24 files), so the 411 figure and one-command claim are accurate \u2014 only the \"32\" is stale. Both records carry it: README.md line 25 (\"golden suite (32 scenarios...\") and the Drive changelog 2026-08-26/27 entry item (6) (\"32 scenarios, one command, 411 tests green\"). Refutation attempts failed: the \"S8b is a sub-variant, so 32 numbered scenarios\" defense is contradicted by commit 8f01fac's own message (\"incl. 8 new regression scenarios\", counting S8b among the 8); git history pins the drift as stale arithmetic \u2014 15af83c had 24 scenarios, 2d748fa grew it to 25 (S1\u2013S25), 8f01fac added 8 = 33, and the README's 32 = 24+8 off the outdated base. Worktree is clean vs HEAD (b2b48c6), so 33 is committed state. Direction is an undercount (understatement, not overstatement), consistent with should_note severity. Suggested action stands: bump 32\u219233 in the README; correct in the next changelog entry per append-only convention, not by editing the old entry."
  },
  {
   "lens": "records-accuracy",
   "severity": "should_note",
   "what": "README's earlier 'State (P0, in progress)' block contradicts the later code-complete block",
   "where": "/Users/christianscholte/aivena-p2am/apps/api/src/amanda-engine/README.md lines 12-14",
   "expected": "A README whose sections agree on whether the outbox worker is wired",
   "actual": "Lines 12-14 still say the worker is 'NOT wired into index.ts yet', but commit 2d748fa wired it (apps/api/src/index.ts:213-231, doubly inert: env flag + per-agency mode) and the later 'P0 CODE-COMPLETE' section describes exactly that. Layered history, understating \u2014 but a top-down reader gets two conflicting states.",
   "suggested_action": "Mark the first State block as superseded (e.g. retitle 'State (P0 mid-build \u2014 superseded below)') or fold it into the code-complete section.",
   "confirmed": true,
   "reason": "Verified against the worktree: README.md lines 12-14 (under a heading still titled 'State (P0, in progress)') assert outbox-worker.ts is 'NOT wired into index.ts yet', while apps/api/src/index.ts lines 213-231 contain the wired worker (engineEnabled() gate, 20s drainAmandaInbound(processTurnDb) interval, 'DOUBLY inert' comment), introduced by commit 2d748fa whose message and diffstat (index.ts +22) explicitly record the wiring. The later 'State (P0 CODE-COMPLETE)' block (line 22) opens with 'Everything above plus', incorporating the stale line by reference instead of superseding it, and the 'Next' section's step 3 (set AMANDA_ENGINE_ENABLED=true on Railway) presumes the wiring exists \u2014 so a top-down reader gets two conflicting states. Refutation attempts failed: the sentence's activation-requirements clause remains true, but its factual head claim is false; and nothing in the file marks the first block as historical/superseded (commit f757857 appended the code-complete block without touching line 13). Discrepancy stands as stated: understating layered history, severity should_note, suggested fix (retitle the first block as superseded or fold it in) is appropriate."
  },
  {
   "lens": "records-accuracy",
   "severity": "cosmetic",
   "what": "Design doc Status header predates P0 code-complete",
   "where": "/Users/christianscholte/aivena-p2am/docs/amanda-automode/DESIGN_v1.2_2026-08-26.md Status line (identical on Drive)",
   "expected": "Status reflecting the current milestone",
   "actual": "'BUILD GO GIVEN ... lean path P0 \u2192 P0.5 \u2192 P1 underway' \u2014 still true (P0.5/P1 pending) and not an overstatement, but it omits that P0 is code-complete and reviewed. Repo snapshot and Drive canonical are byte-identical, so any update must land in both.",
   "suggested_action": "Optionally append 'P0 CODE-COMPLETE 2026-08-26 (38/38 review findings fixed); awaiting Christian's activation approvals' to the Status line in both copies.",
   "confirmed": true,
   "reason": "Verified against the actual files. (1) Line 4 of /Users/christianscholte/aivena-p2am/docs/amanda-automode/DESIGN_v1.2_2026-08-26.md reads exactly \"BUILD GO GIVEN by Christian 2026-08-26 (\\\"get started\\\") \u2014 lean path P0 \u2192 P0.5 \u2192 P1 underway\" with no mention of P0 code-complete anywhere in the doc. (2) MD5 confirms the repo snapshot and the Drive canonical (aivena docs/master doc and changelog/AIVENA_Packet2_AmandaWA_AutoMode_Design_2026-08-26.md) are byte-identical, and docs/amanda-automode/README.md confirms Drive is canonical with the repo as a refresh snapshot \u2014 so any Status update must land in both, as the finding states. (3) P0 is genuinely code-complete and reviewed: apps/api/src/amanda-engine/README.md line 22 states \"State (P0 CODE-COMPLETE, 2026-08-26 late)\" with 38/38 findings fixed; commits 8f01fac (38 findings, 411 tests green) and f757857 corroborate, and v1.3_p0_code_review.json exists beside the design doc. (4) Severity \"cosmetic\" is right: the stale Status understates rather than overstates progress (safe direction for founder trust), and the milestone is honestly recorded in the module README and GO_LIVE_PACK \u2014 just not in the design doc's Status header, which is what the finding targets. No refutation found; the suggested optional append to both copies is the correct fix."
  },
  {
   "lens": "records-accuracy",
   "severity": "cosmetic",
   "what": "Changelog commit range excludes the same-day template-audit commit it describes as 'next'",
   "where": "Drive AIVENA_CHANGELOG.md 2026-08-26/27 entry ('bd56b3e \u2192 f757857' and 'Next = ... Meta template submissions (audit existing approved set first)')",
   "actual": "b2b48c6 (the template audit, pushed, same day) landed after the entry was written; the audit it lists as a future step is already done and captured in GO_LIVE_PACK.md.",
   "expected": "Entry range covering the session's pushed commits, or the audit not listed as pending",
   "suggested_action": "Note b2b48c6 + the completed audit in the next session's changelog entry (append-only convention).",
   "confirmed": true,
   "reason": "Verified against the Drive changelog, the worktree git history, and GO_LIVE_PACK.md. The 2026-08-26/27 changelog entry states the pushed range as \"bd56b3e \u2192 f757857\" and lists \"Meta template submissions (audit existing approved set first)\" as a future step. But b2b48c6 (parent = f757857, i.e. strictly outside the stated range) was committed 2026-08-26 23:09:55 +0200 and pushed to origin/p2-amanda-answer-first the same day, and it IS that audit: its message is \"docs(amanda): template audit vs live approved set...\" and its only change is docs/amanda-automode/GO_LIVE_PACK.md, which now contains \"## Meta templates \u2014 AUDITED against the live approved set (read-only, 2026-08-26)\" (line 58). So the range excludes a same-day pushed commit and the parenthetical audit sub-step listed as pending is already done and captured. The outer item (actual Meta submissions) is genuinely still pending, so severity is correctly cosmetic, and the error understates rather than overstates work. Suggested fix stands: record b2b48c6 + the completed audit in the next session's changelog entry per the append-only convention."
  }
 ],
 "refuted": [],
 "highlights": [
  "\u00a74 mode-in-tool-layer is real and structurally pinned: modes.ts dispatchDecision is the single authority; SHADOW never invokes executeReal (golden S1/S2/S17/R1/R2 prove zero writes incl. escalations and releases); OFF/unknown fail closed. 104 engine tests + 33 golden scenarios green (exceeds the promised 25-30); full repo 656 tests pass (only packages/db/tests/rls.test.ts skips for missing APP_PW env).",
  "\u00a74 confirmation law delivered exactly as promised: book_viewing is deliberately NOT a model tool (tools.ts header documents it); booking executes only via button postback or a conservative multilingual affirmation against exactly ONE unexpired pending action; modifiers ('yes but Sunday') re-ask; declines release; expired never acted on; 'ok no problem' collocations handled (turn.ts + confirmation.ts + S3-S8b).",
  "\u00a74 datetime law: the model never emits absolute datetimes \u2014 deterministic resolver (agency IANA tz, fresh now, DST-safe iterative offset), bare hours 1-7 ambiguous -> clarify, past rejected, explicit echo labels generated server-side and per-slot pending actions so a pick can never book the wrong slot (datetime-resolver.ts, backends-db.ts slotLabel).",
  "\u00a74 EXCLUDE-constraint arbiter + TTL holds delivered in the migration (bookings_active_slot_exclusive with IMMUTABLE wrapper, viewing_slot_holds gist no-overlap); 23P01 mapped to an honest 'slot just taken' path that releases and offers alternatives (process-turn-db.ts, golden S25). Idempotency spine complete: turn_id=f(conversation,MessageSid), unique turn_usage row, send_queue idempotency key, queue UNIQUE(provider_message_id,kind), ask_agency ticket dedupe.",
  "\u00a73b ask-agency semantics conform: ticket is REAL from approval mode up and simulated only in shadow (internal_write class, S16/S17); Amanda keeps the conversation (prompt ladder + openTicketNote); dashboard mirror uses task_type 'amanda_question' which the approve RPC can never text to a buyer; short-code allocator honors the 7-day non-recycle window with a live-code unique index and fresh-tx 23505 retry.",
  "\u00a710 'validators are law': length law, one-question discipline, ES/EN banned-pattern core (13-language LLM-judge tier explicitly deferred in the design itself), and the \u00a711.5 IBAN/payment floor delivered EARLY (design assigned it P1) \u2014 all deterministic in the send path with one constrained regeneration then human queue, buyer never gets an unvetted draft (validators.ts, turn.ts, S9-S13).",
  "\u00a72 gates rebuilt for chat as promised: deterministic turn classifier with social bypass (S14 \u2014 warmth never fact-checked into evasiveness), typed numeric grounding that description prose cannot launder (R3), verifier fail-closed incl. verifier-down blocks fact-bearing sends (S15).",
  "P0 schema matches \u00a78/\u00a711 P0 scope: all 9 tables + viewing outcome columns + viewing_type + disclosure/mute/human-claim columns + amanda_mode dial default 'off', FORCE RLS everywhere, anon/authenticated revoked, SECURITY DEFINER claim RPC with tight grants. Verified against the LIVE DB: 0 of the new tables/columns exist \u2014 the 'PREPARED, NOT APPLIED' record is literally true.",
  "Media Law v0 conforms to the \u00a78 lean scope: EF v3 persists MediaUrl/ContentType, enqueues caption-less media (fixing the v10 silent-skip the CAPTURE_NOTES media audit honestly documents), and the engine asks the buyer to type it \u2014 never auto-acts; STT/vision deferral to the P2 gate is recorded in the design, README, and CAPTURE_NOTES.",
  "Rollout honesty is strong where it counts: go-live pack states ROLLOUT ORDER IS LAW (env before dial) and EF v3 code enforces the matching W4C cutover; every activation step is approval-gated; standing orders intact (no Twilio SID anywhere in engine/docs \u2014 grep-verified; valuation untouched); commit 8f01fac exists and matches the 38/38 review record; v1.3 findings JSON is in the repo as claimed.",
  "Deliberate scope reductions that ARE recorded: ping spine v0 = P2 (README, runbook, \u00a711b), one-tap booking execute endpoint = P2 with the honest interim behavior spelled out in the go-live pack, atomic send-gate move into whatsapp-send-execute before P2 (go-live pack), episodic memory layer 3 (ledger + code comment), disengagement/nudge scheduler (ledger; the pure libs with lifetime-cap columns already exist).",
  "Founder-wish scorecard: (1) intelligent brain = BUILT (sonnet agentic loop, agent-loop.ts/llm.ts, memory layers 1+2 live, layer 3 explicitly deferred in design 11b AND marked in code at process-turn-db.ts:191); (2) sensitive->human = BUILT (handoff_to_human mutes AI + files task + prompt captures substance), no-answer half partial (see discrepancy 3); (3) ask-agency = filing+keep-chatting BUILT, reply-box/relay/SLA honestly SCHEDULED P2 (backends-db.ts:264 'the dedicated Questions from Amanda surface is the P2 build', README P2 list, GO_LIVE_PACK amanda_answer_relay_v1 'needed by P2'); (4) warm-not-pushy = BUILT AS LAW; (5) multi-ping over time = SCHEDULED (ping spine v0 first in 11b ledger, README, runbook 'full ping spine = P2'; amanda_questions.next_ping_at/rung/pings_sent schema-ready); (6) hold-to-quick-answer = SCHEDULED as an honest PROPOSAL with open calls parked for Christian (design 3b-spine status line); (7) graduated modes = BUILT; (8) standard+adjust = read half built (see discrepancy 7); (9) must-not-fail core = BUILT (see discrepancy 4 for the burst edge). Reverse check: NOTHING built contradicts a wish - zero proactive-send machinery exists, so nothing pushy can leave; the one unkeepable promise is discrepancy 2.",
  "Record claims verified by execution: 'golden suite ... one command' and '411 tests green' are EXACTLY true - npx vitest run apps/api = 24 files, 411/411 passed; the amanda-engine subset alone is 8 files/104 tests green including the structural shadow-zero-write pin (modes.test.ts) and all 33 golden scenarios.",
  "Wish 7 (graduated dial) fully conforms: modes.ts dispatchDecision is the single deterministic authority (off fails closed, unknown mode fails closed via exhaustive-never), the model never sees the mode, EF v3 does the per-agency W4C cutover exactly as the pack's behavior matrix states, and agency_settings.amanda_mode defaults 'off' in the migration.",
  "Wish 9 (must-not-fail) core machinery conforms: bookings EXCLUDE constraint as DB-level arbiter (migration lines 103-107) with 23P01 handled as honest 'slot just taken' (process-turn-db.ts:261); booking is deliberately NOT a model tool - only the deterministic confirmation pre-step in turn.ts executes it, with conservative multilingual affirm detection ('yes but Sunday' -> unclear -> re-ask, golden S5/S7/S8b); end-to-end idempotency (turn_id, send_queue idempotency_key ON CONFLICT DO NOTHING, ask_agency 24h ticket dedupe); durable outbox with lease-steal + attempts cap; gates fail closed (verifier down blocks fact-bearing sends, golden S15).",
  "Wish 4 (tone) enforced exactly per the doctrine split 'prompt is aspiration, validators are law': validators.ts implements the length law (3 sentences/35 words, long-form only deterministically EARNED by a get_property_details fetch - turn.ts:175), one-question discipline, mirror band, banned urgency/scarcity/guilt screen with regenerate-then-escalate, and the IBAN/payment floor (11.5) already at P0 (a phase early - promised P1); prompt.ts mirrors it ('Never pushy... If they cool off, you let them breathe').",
  "Wish 3 first half genuinely lands in P0: ask_agency files a REAL ticket from approval mode up (amanda_questions + question_events + dashboard task typed 'amanda_question' so the approve RPC can never text it to the buyer + funnel event), short-code allocator honors the 7-day non-recycle law, the open ticket rides back into every later turn's context ('Q3 to the office ... still waiting'), and the prompt's escalation ladder is Christian's pattern verbatim: check with the office, come back, KEEP HELPING, gather intel naturally via record_lead_intel one light question at a time.",
  "Honest-records posture is strong overall: migration marked PREPARED NOT APPLIED with dry-runs; GO_LIVE_PACK explicitly names the one-tap execute gap, the atomic-send-gate deferral, the ROLLOUT ORDER law, the Send-Pusher dependency-to-verify, and standing orders (valuation gate, no SID); every skip in the worker is a recorded reason, never a silent drop; shadow escalations/tickets are simulated so no agent-visible task can leak from shadow (golden R1/S17).",
  "Media Law v0 conforms to the lean contract: EF v3 enqueues media rows (kind='media') that v10 silently dropped, the engine never auto-acts on unparsed media and warmly asks the buyer to type it (process-turn-db.ts:153-156) - the design's 'voice note containing a cancellation' probe cannot cancel anything.",
  "Day-one data pack (11.4) is real in P0: amanda_funnel_events written at question_ticket/viewing_booked/handoff, turn_usage metered per turn with prompt/model version stamps, viewing outcome columns + viewing_type on bookings, engagement_state + value_nudges_sent (lifetime-cap 2) columns ready for the deferred nudge scheduler.",
  "Template audit (GO_LIVE_PACK) verified against the live whatsapp_templates table read-only: viewing_confirmation_v1, viewing_reminder_v1, viewing_followup_v1, missed_call_recovery_v1 are all status='approved' AND provider_status='approved' in exactly 13 languages each; viewing_confirmation_v1 variables are exactly {{1}} buyer_first_name {{2}} agency_name {{3}} date {{4}} time {{5}} property_ref as claimed; the three 'genuinely new' keys (amanda_answer_relay_v1, first_touch_lead_v1, viewing_morning_of_v1) do not exist live \u2014 the reuse-never-duplicate claim is honest.",
  "GO_LIVE_PACK dry-run SQL executed live (read-only): 1a returns 0 overlapping bookings (query is type-safe \u2014 bookings.agent_id is text, so the COALESCE works), 1b returns 0 table collisions, and none of the columns the migration adds (bookings.viewing_type/outcome*, conversations.ai_*, agency_settings.amanda_mode/amanda_settings) exist yet; booking_status enum contains requested/confirmed/rescheduled; the migration would apply cleanly today.",
  "Review-count claims exact: v1.3_p0_code_review.json holds 38 confirmed + 1 refuted = '39 raised, 38 confirmed'; commits bd56b3e, 15af83c, 2d748fa, 8f01fac, f757857, b2b48c6 all exist and the branch is pushed and in sync with origin/p2-amanda-answer-first \u2014 'committed + pushed' is true.",
  "All four 'demo-killer' descriptions in the changelog match confirmed critical findings verbatim (create_manual_viewing require_role GUC; buyer text in the column the approve RPC sends; send_queue 'pending' not a send_status label; STABLE expression in the EXCLUDE), and each fix is present in current code: role GUCs set via set_config before the RPC (process-turn-db.ts:222-227), message_body now carries the draft (queueDraft comment + insert), sendReply omits status so the enum default 'queued' applies, and the migration wraps the range in IMMUTABLE booking_slot_range().",
  "Test claims verified by running them: npx vitest run apps/api \u2192 exactly 411 passed (the README/changelog figure); the amanda-engine subtree alone is 104 tests / 8 files, all green; the golden one-command works; the only failure in a full-repo run is packages/db/tests/rls.test.ts, which requires the APP_PW env var \u2014 pre-existing, environment-gated, unrelated to the engine.",
  "Doc sync perfect: repo DESIGN_v1.2_2026-08-26.md is byte-identical to the Drive canonical AIVENA_Packet2_AmandaWA_AutoMode_Design_2026-08-26.md, and the Drive GO_LIVE_PACK_snapshot.md is byte-identical to the repo pack (includes the b2b48c6 template audit); all four findings JSONs (v1.0/v1.1/v1.2/v1.3) exist in both the repo and the Drive amanda-automode-materials folder.",
  "Runbook mechanics that DO work: the big-red-switch UPDATE targets a real column (agency_settings.agency_id exists; amanda_mode 'off' is in the migration's CHECK), the cost SQL matches amanda_turn_usage's actual columns (created_at/input_tokens/output_tokens), '[amanda-engine]' is the real log token, and the 20s drain claim matches AMANDA_ENGINE_TICK_MS=20_000 in apps/api/src/index.ts.",
  "GO_LIVE_PACK step preconditions and demo claims hold in code: EF v3 file exists at the stated path with the exact per-agency W4C cutover matrix described; tickets and booking confirmations use their own task types ('amanda_question', 'amanda_booking_confirm') and /tasks renders ALL open dashboard_tasks with humanized labels for unknown types (operations.ts actionQueue has no task_type filter) \u2014 'visible on /tasks' is true; send_queue rows carry requested_by='amanda_engine' as stated; the 24h window is pre-checked at enqueue (23h30m margin + 30-min expiry); nudgeCalendarSync() rides the existing calendar sync after booking.",
  "CAPTURE_NOTES.md confirms the changelog's media-audit claim: W4C's draft trigger requires non-empty body text, so an uncaptioned voice note is stored then silently ignored by the AI path \u2014 the claim is a faithful description of captured v10 behavior.",
  "The rollout law ('AMANDA_ENGINE_ENABLED=true BEFORE any agency leaves off/shadow') is consistent with the actual EF v3 code path it warns about, and the standing orders in 'What stays OFF regardless' match memory and repo state (valuation gate untouched, engine rides the live executor, no Twilio SID anywhere in the engine code)."
 ]
}