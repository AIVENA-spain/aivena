node_modules
.env
.env.local
.env.production
dist
build
.DS_Store
*.log
packages/db/client.ts.backup

# Tooling — kept local intentionally (not an oversight):
# .claude/ holds local Claude Code session/agent config; .mcp.json holds local MCP
# server config. Both are machine-specific and deliberately not committed.
.claude/
.mcp.json
